server:
  port: 8082

spring:
  application:
    name: settlement-worker
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # PC1에서 개발/로컬 실행 시 127.0.0.1. PC3에 배포하면 PC1의 LAN IP(192.168.0.5)로
    # 바꿔야 한다 — MySQL이 원격 접속을 허용하도록 bind-address/사용자 권한 사전 확인 필요.
    url: jdbc:mysql://127.0.0.1:3306/core2_spa?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        jdbc.batch_size: 100
  rabbitmq:
    host: 192.168.0.6
    port: 5672
    username: coreapp
    password: 1234
    virtual-host: core_vhost
    listener:
      simple:
        # M2 범위: 자동 ack. 수동 ack + 자동 DLQ 배선은 Phase 4에서 추가.
        acknowledge-mode: auto

management:
  endpoints:
    web:
      exposure:
        include: health
